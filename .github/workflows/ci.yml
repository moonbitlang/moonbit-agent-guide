name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        channel: [nightly, latest]
    name: test (${{ matrix.channel }})
    steps:
      - uses: actions/checkout@v4

      - name: Install MoonBit (${{ matrix.channel }})
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash -s ${{ matrix.channel }}
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"

      - name: moon version
        run: moon version --all

      - name: moon test moonbit-agent-guide/SKILL.mbt.md
        run: |
          moon check moonbit-agent-guide/SKILL.mbt.md --deny-warn --no-render
          moon test moonbit-agent-guide/SKILL.mbt.md --deny-warn --no-render

      - name: Test language fundamentals
        run: |
          moon check moonbit-agent-guide/references/moonbit-language-fundamentals.mbt.md --deny-warn --no-render
          moon test moonbit-agent-guide/references/moonbit-language-fundamentals.mbt.md --deny-warn --no-render
